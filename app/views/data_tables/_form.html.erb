<% if @data_table.errors.any? %>

    <div class="alert alert-error alert-block"> <a class="close" data-dismiss="alert" href="#">×</a>
  <h4 class="alert-heading">Error!</h4>
  Hubo un problema al guardar la tabla, completaste todos los datos? </div>
    <%end%>



<%= form_for(@data_table, :html => {:class => "form-horizontal"}) do |f|  %>
    <div class="control-group">
      <label class="control-label">Nombre de la tabla :</label>
      <div class="controls">
        <%= f.text_field :name, :class => "span20" %></div>
    </div>

    <div class="form-actions">
      <%= f.submit 'Crear tabla', :class => 'btn btn-success' %>
    </div>
<% end%>